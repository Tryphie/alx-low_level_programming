#include "main.h"
#include <stdio.h>

/**
 * print_rev - imprime en reversa
 * @s: string
 * return: Void
 */

void print_rev(char *s)

{
	int longi = 0;

	while (s[c] != '\0')
	{
	c++;
	}

	for (c -= 1; c >= 0; c--)
	{
	_putchar(s[c]);
	}
	_putchar('\n');
}
